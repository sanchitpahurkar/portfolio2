/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'sanchitpahurkar.s3.us-east-1.amazonaws.com'
        ],
    },
};

export default nextConfig;
